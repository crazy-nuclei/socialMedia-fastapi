name: Build and Deploy Code 

on: ["push", "pull_request"]

jobs: 
  job1:
    runs-on: macos-latest
    steps: 
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: say hi to Aayush 
        run: echo "Hello Aayush"